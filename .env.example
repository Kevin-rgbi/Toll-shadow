# Development only. Enables the synthetic visualization prototype in the local dev server.
# A production build fails when this is explicitly true, and built artifacts never ship
# public/data/demo fixtures.
VITE_USE_DEMO_DATA=true

# Basemap uses OpenStreetMap raster tiles through MapLibre (no API key required).
